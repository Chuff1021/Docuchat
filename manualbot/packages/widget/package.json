{
  "name": "@manualbot/widget",
  "version": "1.0.0",
  "description": "ManualBot embeddable chat widget",
  "main": "dist/widget.js",
  "types": "dist/widget.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "node build.js",
    "build:dev": "node build.js --watch",
    "watch": "node build.js --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2"
  }
}
